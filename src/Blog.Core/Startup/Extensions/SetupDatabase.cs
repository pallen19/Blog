namespace Blog.Core.Startup.Extensions;
public static class SetupDatabaseExtensions{

    /* public static void SetupDatabase(this IServiceCollection services, EnvironmentSettings environmentSettings){
        services.AddTransient<BlogDbContext>();
        //connection string we will pull from appsettings
        //username=postgres
        //password=postgres
        services.AddDbContext<BlogDbContext>(options =>
            options.UseNpgsql("")); */
    }

